<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/suggestion_card_view"
    card_view:cardCornerRadius="4dp"
    android:layout_margin="5dp"
    card_view:elevation="@dimen/elevation_amount">
    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content">

        <RelativeLayout
            android:layout_width="fill_parent"
            android:layout_height="@dimen/card_height"
            android:id="@+id/card_image_title_holder_layout">
        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="@dimen/card_height"
            android:id="@+id/suggestion_iv"
            android:scaleType="centerCrop">
        </ImageView>

        <TextView
            style="@style/large_text_view"
            android:layout_width="fill_parent"
            android:layout_height="50dp"
            android:id="@+id/suggestion_card_title"
            android:gravity="bottom"
            android:layout_margin="0dp"
            android:padding="5dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:background="@color/bg_light_grey_transparent" />
        </RelativeLayout>

    <TextView
        style="@style/normal_text_view"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/suggestion_card_description"
        android:layout_below="@+id/card_image_title_holder_layout"
        />

    </RelativeLayout>
</android.support.v7.widget.CardView>